# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

name: bookdown

jobs:
  bookdown:
    runs-on: ubuntu-latest
    # Only restrict concurrency for non-PR jobs
    concurrency:
      group: pkgdown-${{ github.event_name != 'pull_request' || github.run_id }}
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:

      - uses: actions/checkout@v2

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-renv@v2

      # - uses: r-lib/actions/setup-r-dependencies@v2
      #   with:
      #     cache-version: 2
      #     extra-packages: |
      #       any::rmarkdown
      #       any::bookdown
      #       any::knitr
      #       any::printr
      #       any::dplyr
      #       any::magrittr
      #       any::tidyr
      #       any::readr
      #       any::purrr
      #       any::tibble
      #       any::kableExtra
      #       any::ape
      #       any::maps
      #       any::vegan
      #       any::labdsv"
      #       any::ggplot2
      #       any::rcmdcheck
      #     needs: |
      #     website
      #     coverage

      - name: Cache bookdown results
        uses: actions/cache@v2
        with:
          path: _bookdown_files
          key: bookdown-${{ hashFiles('**/*Rmd') }}
          restore-keys: bookdown-

      - name: Build site
        run: bookdown::render_book("index.Rmd", quiet = TRUE)
        shell: Rscript {0}

      - name: Deploy to GitHub pages 🚀
        if: github.event_name != 'pull_request'
        uses: JamesIves/github-pages-deploy-action@4.1.4
        with:
          branch: gh-pages
          folder: _book

      #
      # - name: Render Book as gitbook
      #   run: Rscript -e 'library("bookdown");bookdown::render_book("index.Rmd", "bookdown::gitbook")'
      #
      # - name: Render Book as epub
      #   run: Rscript -e 'library("bookdown");bookdown::render_book("index.Rmd", "bookdown::epub_book")'
